CREATE TABLE user(
    userID int NOT NULL AUTO_INCREMENT,
    userName varchar(255) NOT NULL,
    userEmail varchar(255) NOT NULL,
    userUserName varchar(255) NOT NULL,
    userPassword varchar(255) NOT NULL,
    userCardNumber varchar(255) NOT NULL,
    PRIMARY KEY (userID)
);